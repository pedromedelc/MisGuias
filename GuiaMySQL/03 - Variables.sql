-- Declarar Variables
/* Las variables declaradas por el usuario existiran solo en la sesion
que son declaradas */

SET @nombre = "Pedro"; -- Se declara una variable de Tipo cadena
SET @numero = 1; -- Se declara una variable de tipo entero
SET @decimal = 1.5; -- SE declara una variable de tipo decimal

